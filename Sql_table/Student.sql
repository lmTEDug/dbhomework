CREATE TABLE Student(
  sno NUMERIC(6),
  sname VARCHAR(10) NOT NULL,
  sex VARCHAR(2),
  brithdate DATE,
  place VARCHAR(10),
  dept VARCHAR(20),
  major VARCHAR(20),
  sgrade VARCHAR(2),
  PRIMARY KEY(sno));

